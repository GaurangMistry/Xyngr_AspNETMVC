@model IEnumerable<xyngr.Models.CommentWriterViewModel>
@Styles.Render("~/Content/GridCSS")

<section class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-head">
                <div class="card-header">
                    <h4 class="card-title">All Comment Writers</h4>
                    <div class="messageDiv">
                        <label>@ViewBag.Message</label>
                    </div>
                    <a class="heading-elements-toggle"><i class="la la-ellipsis-h font-medium-3"></i></a>
                    <div class="heading-elements">
                        <button class="btn btn-primary btn-sm" onclick="javascript:window.location.href='/Admin/CommentWriter/Create'"><i class="ft-plus white"></i>Add CommentWriter</button>
                    </div>
                </div>
            </div>

            <div class="card-content">
                <div class="card-body">
                    <div class="table-responsive">
                        <table id="commentwriter-contacts" class="table table-white-space table-bordered row-grouping display no-wrap icheck table-middle">
                            <thead>
                                <tr>
                                    <th>First Name</th>
                                    <th>Last Name</th>
                                    <th>Email</th>
                                    <th>Phone No</th>
                                    <th></th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>

                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td width="20%">
                                            @item.FirstName
                                        </td>
                                        <td width="20%">
                                            @item.LastName
                                        </td>
                                        <td width="20%">
                                            @item.Email
                                        </td>
                                        <td width="20%">
                                            @item.PhoneNo
                                        </td>
                                        <td width="10%">
                                            @if (item.Status)
                                            {
                                                <span class="active">Activate</span>
                                            }
                                            else
                                            {
                                                <span class="danger">Inactivate</span>
                                            }
                                        </td>
                                        <td width="10%">
                                            @Html.ActionLink("Edit", "Edit", "CommentWriter", new { id = item.CommentWriterID }, null)
                                            | @Html.ActionLink("Delete", "Delete", "CommentWriter", new { id = item.CommentWriterID }, new { onclick = "return DeleteAlert();" })
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
@Scripts.Render("~/bundles/GridJS")

<script src="~/Scripts/Admin/CommentWriter/CommentWriterList.js"></script>
<script type="text/javascript">
    function DeleteAlert() {
        return confirm("Are you sure you want to delete record?");
    }
</script>
